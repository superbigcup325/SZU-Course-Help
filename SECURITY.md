# 安全说明

## 敏感数据

请勿在 Issue、Pull Request、日志或截图中提交以下内容：

- 深圳大学学号与密码
- Cookie、token、vtoken 和验证码坐标
- `card_signing_private.pem`、`card_signing_public.pem` 或任何 Card Key
- 本地 SQLite 数据库、OCR 临时图片和完整运行日志

仓库已通过 `.gitignore` 排除这些文件，但提交者仍应在推送前检查 `git status` 和暂存区内容。

## 漏洞报告

安全问题请优先通过 GitHub 仓库所有者的私密联系方式报告，不要直接创建包含利用细节或凭据的公开 Issue。报告应包含受影响版本、复现条件、潜在影响和建议修复方向，并使用脱敏数据。

## 已知边界

- 项目只监听本机 `127.0.0.1`，不适合作为公网服务部署。
- 学校登录与选课接口当前由学校提供为 HTTP，传输安全能力受上游系统限制。
- 学校密码仅保存在当前 Python 进程内存中，用于会话过期后的 OCR 自动重登录。
- 学校密码、token 与 Cookie 不写入 SQLite、浏览器存储或磁盘会话文件；关闭进程或开发重载后需要重新手动登录。
- WebVPN 只作为认证后的只读查询备用入口，受控浏览器使用一次性临时配置；选课提交固定发送到学校主站且不跨后端重试，本版不提供自动退课。
- Release 的清单、课程缓存和 Card Key 身份位于平台用户数据目录；旧版迁移只复制经过校验的文件且绝不覆盖已有目标。
- Card Key V3 使用本机 Ed25519 密钥签名；私钥一旦泄露，应立即更换并视所有旧 Card Key 为不可信。
- 自动化测试禁止访问真实学校接口，但人工运行 `main.py` 会连接学校系统。

## 支持范围

仅维护本仓库最新 `main` 分支。使用旧打包文件前，请先确认其版本与源码一致。
