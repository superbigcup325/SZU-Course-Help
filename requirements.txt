# Runtime dependencies for Python 3.13+
fastapi>=0.116.1
uvicorn>=0.35.0
requests>=2.32.5
chardet>=5.0.0,<6
pycryptodome>=3.23.0

# OCR is required for automatic re-login after the school session expires.
ddddocr>=1.6.1,<1.7
opencv-python>=4.10.0
onnxruntime>=1.20.0

# Optional PaddleOCR fallback (disabled unless COURSE_SELECT_USE_PADDLE_OCR=1):
# paddleocr>=3.4.0
# paddlepaddle>=3.1.1

# Development only:
# pytest>=8.0.0
# httpx>=0.27.0
# httpx2>=2.0.0
# ruff>=0.15.0,<1
# nuitka>=2.8.9
# zstandard>=0.25.0
